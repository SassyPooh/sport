import { VoyReplacerPipe } from './voy-replacer.pipe';

describe('VoyReplacerPipe', () => {
  it('create an instance', () => {
    const pipe = new VoyReplacerPipe();
    expect(pipe).toBeTruthy();
  });
});
